# ==== VPN / Gluetun Basic Configuration ====
# Provider, e.g.: protonvpn, mullvad, custom. Required
VPN_SERVICE_PROVIDER=custom
# WireGuard private key (sensitive, do not commit)
WIREGUARD_PRIVATE_KEY=
# Pre-shared key (optional)
WIREGUARD_PRESHARED_KEY=
# WireGuard address assigned to the container, usually /32 (IPv4) or /128 (IPv6)
# Example: 10.8.0.2/32, fd01:beee:beee::2/128
WIREGUARD_ADDRESSES=

# Region/country filter (comma-separated). Can set one or both
SERVER_REGIONS=Europe
SERVER_COUNTRIES=Germany

# Outbound allowed local subnets (comma-separated), common private networks: 192.168.0.0/16,10.0.0.0/8
FIREWALL_OUTBOUND_SUBNETS=

# ==== Advanced Configuration ====

# VPN Network Subnet (must not overlap with local network)
VPN_NETWORK_SUBNET=172.32.0.0/16
VPN_NETWORK_SUBNET_IPV6=fd00:32::/64

# VPN Network Gateway IPs
VPN_NETWORK_GATEWAY=172.32.0.1
VPN_NETWORK_GATEWAY_IPV6=fd00:32::1

# Service IP Addresses within the VPN Network
GLUETUN_IP_ADDRESS=172.32.0.2
GLUETUN_IPV6_ADDRESS=fd00:32::2

WG_EASY_IP_ADDRESS=172.32.0.4
WG_EASY_IPV6_ADDRESS=fd00:32::4

