version: "3"

services:
  wg-easy:
    build:
      dockerfile: ./Dockerfile
    ports:
      - "51820:51820/udp"
      - "51821:51821/tcp"
    networks:
      vpn:
        ipv4_address: 172.31.0.8 # Fixed IPv4 address
        ipv6_address: "fd01:beee:beee::8" # Fixed IPv6 address
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv4.conf.all.src_valid_mark=1
      - net.ipv6.conf.all.disable_ipv6=0
      - net.ipv6.conf.all.forwarding=1
      - net.ipv6.conf.default.forwarding=1
    env_file:
      - .env
    volumes:
      - /lib/modules:/lib/modules:ro
      - wg-easy-data:/etc/wireguard # Persist WireGuard configuration
    restart: unless-stopped

  gluetun:
    build:
      dockerfile: ./Dockerfile.gluetun
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    devices:
      - /dev/net/tun:/dev/net/tun
    env_file:
      - .env
    volumes:
      - /lib/modules:/lib/modules:ro
      - gluetun-data:/gluetun # Persist gluetun configuration
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv4.conf.all.src_valid_mark=1
      - net.ipv6.conf.all.disable_ipv6=0
      - net.ipv6.conf.all.forwarding=1
      - net.ipv6.conf.default.forwarding=1
    networks:
      vpn:
        ipv4_address: 172.31.0.4 # Fixed IPv4 address
        ipv6_address: "fd01:beee:beee::4" # Fixed IPv6 address
    restart: unless-stopped

volumes:
  gluetun-data:
  wg-easy-data:

networks:
  vpn: # separate bridge network with IPv4 and IPv6
    name: vpn
    driver: bridge
    enable_ipv6: true
    ipam:
      config:
        - subnet: 172.31.0.0/16 # IPv4 subnet
          gateway: 172.31.0.1 # IPv4 gateway
        - subnet: fd01:beee:beee::/48 # IPv6 subnet
          gateway: fd01:beee:beee::1 # IPv6 gateway